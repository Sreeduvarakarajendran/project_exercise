 <%- include('partials/header.ejs') %>
 <section class="form-section mt-4">
        <form action="/submit" method="POST" class="mx-auto" style="max-width:700px;">
            <!-- <h2 class="mb-3 text-center">Write your blog about a favorite bike</h2>
            <div class="mb-3">
                <label for="author" class="form-label">Author name</label>
                <input id="author" name="title" type="text" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="w3review" class="form-label">Create your bike blog</label>
                <textarea id="w3review" name="content" rows="4" class="form-control"></textarea>
            </div> -->
            <div class="text-center">
                <button type="submit" class="btn btn-primary" value="chicken" name="choice">🍗</button>
                <button type="submit" class="btn btn-primary" value="beef" name="choice">🥩</button>
                <button type="submit" class="btn btn-primary" value="fish" name="choice">🐟</button>
            </div>
        </form>
    </section>
    <% if(!data) { %>
        <div class="result-section mt-4 text-center">
            <p>Your favorite cuisine will be displayed here once you make a choice!</p>
        <% } else { %>
             <p>Name: <%= data.name %></p>
            <p>Location: <%= data.location %></p>
            <p>Rating: <%= data.rating %></p>
             </div>
        <% } %>
<%- include('partials/footer.ejs') %>